window.onload = function () {
    require('./sliders');
    require('./popups');
    require('./validation');
    require('./animations');
    require('./file-upload');

    require('./makedinput')
};
